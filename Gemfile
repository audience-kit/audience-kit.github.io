source "https://rubygems.org"

# Matches the Jekyll version and plugins GitHub Pages uses to build this site.
gem "github-pages", group: :jekyll_plugins

# Required on Ruby 3.x, which no longer bundles these.
gem "webrick"
gem "csv"
gem "base64"
gem "bigdecimal"
gem "logger"
